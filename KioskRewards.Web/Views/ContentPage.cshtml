@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentPage>
@{
    Layout = "~/Views/_Layout.cshtml";
    ViewData["Title"] = string.IsNullOrWhiteSpace(Model.MetaTitle) ? Model.Name : Model.MetaTitle;
    ViewData["MetaDescription"] = Model.MetaDescription;
}

<article>
    <h1>@(string.IsNullOrWhiteSpace(Model.Heading) ? Model.Name : Model.Heading)</h1>

    @if (Model.BodyText is not null)
    {
        <div class="body">@Model.BodyText</div>
    }

    <div class="contact-info-blocks">
        @await Html.GetBlockListHtmlAsync(Model.ContactInfoBlocks)
    </div>
</article>
